language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t parvindran/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true parvindran/docker-react npm run test -- --coverage